# Backend API URL (used by app for login, API calls)
# For local dev: use localhost. For physical device: use your machine's LAN IP (e.g. http://192.168.1.100:3000)
EXPO_PUBLIC_API_URL=http://localhost:3000
