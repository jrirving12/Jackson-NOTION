# Database (required for API; use Railway PostgreSQL or local Postgres)
DATABASE_URL=postgresql://user:password@localhost:5432/tequila_crm

# Auth (required in production)
JWT_SECRET=your-secret-at-least-32-chars-long

# Server
PORT=3000
NODE_ENV=development

# Frontend URL for CORS (Expo web / iOS app origin)
FRONTEND_URL=http://localhost:8081
